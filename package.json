{
  "name": "sudoku-kata",
  "version": "1.0.0",
  "description": "",
  "module": "src/main.ts",
  "type": "module",
  "author": "gustaveWPM (github.com/gustaveWPM)",
  "license": "MIT",
  "private": true,
  "scripts": {
    "prepare": "husky install",
    "typecheck": "tsc --noEmit --pretty",
    "bundle": "bun typecheck && bun lint && bun build ./src/main.ts --outdir ./dist --minify",
    "make": "bun install && bun bundle",
    "clean": "rm -rf dist",
    "fclean": "bun clean && rm -rf node_modules",
    "re": "bun fclean && bun install && bun make",
    "start": "bun run dist/main.js",
    "dev": "bun --watch run src/main.ts",
    "ts-node": "bun run src/main.ts",
    "commit": "git-cz",
    "format": "biome format --write .",
    "ts-prune": "ts-prune | .ts-prune/sanitize.py --quiet",
    "ts-prune-verbose": "ts-prune | .ts-prune/sanitize.py",
    "lint": "bun biome lint .",
    "test": "bun test",
    "coverage": "bun test --coverage"
  },
  "devDependencies": {
    "@biomejs/biome": "1.4.1",
    "@commitlint/config-conventional": "^18.1.0",
    "@commitlint/cz-commitlint": "^18.2.0",
    "@types/jest": "^29.5.11",
    "bun-types": "^1.0.16",
    "commitizen": "^4.3.0",
    "commitlint": "^18.4.3",
    "editorconfig": "^2.0.0",
    "husky": "^8.0.3",
    "jest": "^29.7.0",
    "jest-mock": "^29.7.0",
    "lint-staged": "^15.0.2",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.2",
    "ts-prune": "^0.10.3",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.3.3"
  }
}
